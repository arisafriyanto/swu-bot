<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="shortcut icon" href="./../assets/images/swu.png" type="image/png" />
    <title>Login</title>
    <link rel="stylesheet" href="./../assets/styles.css" />
  </head>
  <body class="font-inter">
    <main class="bg-gray-100">
      <div class="flex flex-col items-center justify-center h-dvh px-6 py-8 lg:py-0">
        <a href="/login" class="flex items-center mb-6">
          <div class="flex items-center">
            <div>
              <img class="w-16 h-16 text-white p-[5px]" src="./../assets/images/swu.png" alt="Logo SWU" />
            </div>
            <div class="ml-2 text-center">
              <span class="block text-lg font-bold leading-tight tracking-tight text-gray-900 md:text-[20px] -mb-1">STMIK WIDYA UTAMA</span>
              <span class="block text-base font-light">Smart Winner Universe</span>
            </div>
          </div>
        </a>

        <div class="w-full bg-white rounded-2xl shadow md:mt-0 sm:max-w-md">
          <div class="p-6 space-y-4 md:space-y-6 sm:p-8">
            <h1 class="text-xl font-bold leading-tight tracking-tight text-gray-900 md:text-2xl">Masuk ke akun Anda</h1>
            <form class="space-y-4 md:space-y-6" action="/login" method="POST">
              <div>
                <label for="username" class="block mb-2 text-sm font-medium text-gray-900">Username</label>
                <input
                  type="text"
                  name="username"
                  id="username"
                  class="text-gray-900 bg-gray-50 border border-gray-300 sm:text-sm rounded-lg focus:outline-none focus:ring focus:ring-blue-300 focus:border-blue-500 transition duration-200 shadow-sm block w-full p-2 sm:p-2.5"
                  placeholder="admin"
                  required=""
                />
                <% if(error) { %>
                <span class="text-red-600 text-sm ml-1"><%=error%></span>
                <% } %>
              </div>
              <div>
                <label for="password" class="block mb-2 text-sm font-medium text-gray-900">Password</label>
                <input
                  type="password"
                  name="password"
                  id="password"
                  placeholder="••••••••"
                  class="text-gray-900 bg-gray-50 border border-gray-300 sm:text-sm rounded-lg focus:outline-none focus:ring focus:ring-blue-300 focus:border-blue-500 transition duration-200 shadow-sm block w-full p-2 sm:p-2.5"
                  required=""
                />
              </div>

              <button
                type="submit"
                class="w-full text-white bg-[#008fff] hover:bg-[#007fe3] focus:ring-4 focus:outline-none focus:ring-blue-300 font-semibold rounded-lg text-sm px-5 py-2.5 text-center"
              >
                Login
              </button>
              <p class="text-sm font-light text-gray-500 text-center">&copy; 2024 Skripsi Aris Apriyanto.</p>
            </form>
          </div>
        </div>
      </div>
    </main>
  </body>
</html>
